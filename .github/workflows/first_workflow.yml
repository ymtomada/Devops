name: workflow1
on:
  push:
    branches: [ "main"]
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: run a single command on ubuntu machine
        run: hostname
      - name: step 2
        run: echo "Step 2 running"
      - name: step 3
        run: echo "Step 3 running"
