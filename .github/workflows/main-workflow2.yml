name: main-workflow2
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
jobs:
  build:
    runs-on: my-server
    steps:
      - name: Run a one-line script
        run: echo Hello, world!
